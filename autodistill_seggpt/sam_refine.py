import numpy as np
import supervision as sv

def refine_detections(img:np.ndarray,detections:sv.Detections):
    # TODO use each detection mask (or bbox) as a prompt for SAM
    raise NotImplementedError("TODO")